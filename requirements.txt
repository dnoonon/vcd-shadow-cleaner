# VMware Cloud Director Shadow VM Cleanup Tool
# Python dependencies

# GUI framework (cross-platform)
PySide6>=6.5.0

# HTTP client for VCD API
requests>=2.28.0

# Optional: for better SSL handling
urllib3>=2.0.0

# Secure credential storage (OS keychain: macOS Keychain, Windows Credential Manager, Linux Secret Service)
keyring>=24.0.0

# Optional: for creating binaries
PyInstaller>=5.0.0